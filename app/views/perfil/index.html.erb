<h1>Perfil#index</h1>
<p>Find me in app/views/perfil/index.html.erb</p>

   <% if current_user.role == "admin" %> 
      <%= render 'perfil/admin' %>
   <% else %>
     <%= current_user.name %> 
     <br>
      <%= current_user.email %>
   <%end %>